# Python Interview Questions & Answers

## Adhikya Edammala: Can we access protected variable outside class?

Yes. Protected variables use single underscore 

## Allanki VV Manikanta Sai: Use of top command

top command is used to:
 * monitor CPU usage
 * monitor memory usage
 * view running processes

## Arpit Yadav: Multiple arguments in python while defining

Example:

def add(a, b, c):
    return a + b + c

## Boorle Sowmya Sri Lakshmi: Decorators in python

Decorators are used to modify function behavior.

## Injamuri Arun Kumar: List comprehension

numbers = [x for x in range(5)]

## Kamparapu Lakshman: Use of else in for loop

else executes when loop finishes normally.

## Md Musharaf: Return type of sorted() function

sorted() returns a list.

## Naman Sharma: Use of virtual environment

creates isolated Python environments for projects.

## Nayunipatruni Harsha Vardhan: Difference between strip and split

strip()
- removes spaces

split()
- breaks string

## Parlapalli Sulochana: Default type of keyword argument

Keyword arguments are passed using parameter names.

## Rongala Vasu: Types of operators in python

Example:

+, -, *, /, ==.

## Rovinpal Udupi: Use pip freeze

pip freeze shows installed Python packages.

## Tumma Haritha: How garbage collection done in python

Python automatically removes unused objects using:

## Vikas Mehta: Why strings are immutable in python

Strings are immutable for:
- security
- memory optimization

## Yash Nitin Raut: Why we use constructor in python

Constructor initializes object data automatically when object is created.

